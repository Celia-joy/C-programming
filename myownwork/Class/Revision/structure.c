#include <stdio.h>
/*struct Employee {
	char name[20];
	int age;
	char location[20];
};
struct Address{
	char city[20];
	char country[20];
};
int main (){
	struct Employee E;

	struct Student *ptr = &S;
	struct Address address;
	printf("Enter your name: ");
	scanf("%s", ptr->name);
	printf("Enter your age: ");
	scanf("%d", &ptr->age);
	printf("Enter your city: ");
	scanf("%s", address.city);
	printf("Enter your country: ");
	scanf("%s", address.country);
	printf("Your name , age and address (city and country) are: %s, %d, %s, %s ", ptr->name, ptr->age, address.city, address.country);
	return 0;	
}*/
/*struct E {
    char a;    // 1 byte
    short b;   // 2 bytes  ? must start at multiple of 2
    int c;     // 4 bytes  ? must start at multiple of 4
    double d;  // 8 bytes  ? must start at multiple of 8
    char e;    // 1 byte
};
int main(){
	struct E e;
    printf("The size of B is %zu", sizeof(e));
    return 0;
}*/
struct Car{
	
};
